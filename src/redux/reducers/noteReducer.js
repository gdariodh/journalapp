import { types } from "../types";

const initialState = {
    notes: [],
    active: null,
}

export const authReducer = (state = initialState, action) => {
  switch (action.type) {

    default:
      return state;
  }
};
